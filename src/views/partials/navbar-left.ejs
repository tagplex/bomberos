<aside class="main-sidebar sidebar-dark-primary elevation-4">
  <!-- Brand Logo -->
  <a href="#" class="brand-link">
    <img src="/img/favicon_3.webp" alt="Logo T. Muestras" class="brand-image img-circle elevation-0" style="opacity: .8">
    <span class="brand-text font-weight-dark">Salud Quemchi</span>
  </a>
  <% if(locals.isLogged) { %>
    <!-- Sidebar -->
    <div class="sidebar">
      <!-- Sidebar user panel (optional) -->
      <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        <div class="image">
          <img src="/img/profile_image.webp" class="img-circle elevation-2" alt="User Image">
        </div>
        <div class="info">
          <a href="/users/profile" class="d-block">Hola, <b><%= locals.userLogged.first_name.substr(0, 1) %>.
              <%= locals.userLogged.last_name.substr(0, 6)%></b> <i class="fas fa-user-edit"></i></a>
        </div>
      </div>
      <% } %>
        <!-- SidebarSearch Form -->
        <div class="form-inline">
          <div class="input-group" data-widget="sidebar-search">
            <input class="form-control form-control-sidebar" type="search" placeholder="Search" aria-label="Search">
            <div class="input-group-append">
              <button class="btn btn-sidebar">
                <i class="fas fa-search fa-fw"></i>
              </button>
            </div>
          </div>
        </div>

        <!-- Sidebar Menu -->
        <nav class="mt-2">
          <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
            <!-- Add icons to the links using the .nav-icon class
             with font-awesome or any other icon font library -->
            <% if(locals.isLogged) { %>
              <% if(locals.userLogged.id_rol == 1) { %>
                <li class="nav-item">
                  <a href="#" class="nav-link">
                    <i class="fa-solid fa-user-lock nav-icon"></i>
                    <p>
                      Administrador
                      <i class="fas fa-angle-left right"></i>
                    </p>
                  </a>
                  <ul class="nav nav-treeview">
                    <li class="nav-item">
                      <a href="/users/view" class="nav-link">
                        <i class="fa fa-user-shield nav-icon"></i>
                        <p>Usuarios</p>
                      </a>
                    </li>
                    <li class="nav-item">
                      <a href="/patients/view" class="nav-link">
                        <i class="fa fa-users nav-icon"></i>
                        <p>Pacientes</p>
                      </a>
                    </li>
                  </ul>
                </li>
                <% } %>
                  <% } %>
                  <% if(locals.isLogged) { %>
                    <% if(locals.userLogged.id_rol == 1 || locals.userLogged.id_rol == 2) { %>
                    <li class="nav-item">
                      <a href="#" class="nav-link">
                        <i class="fa-solid fa-vial nav-icon"></i>
                        <p>
                          Exámenes
                          <i class="fas fa-angle-left right"></i>
                        </p>
                      </a>
                      <% } %>
                  <% } %>
                      <ul class="nav nav-treeview">
                        <% if(locals.isLogged) { %>
                          <% if(locals.userLogged.id_rol == 1) { %>
                            <li class="nav-item">
                              <a href="/results/upload" class="nav-link">
                                <i class="fa fa-upload nav-icon"></i>
                                <p>Publicar</p>
                              </a>
                            </li>
                            <li class="nav-item">
                              <a href="#" class="nav-link">
                                <i class="fa-solid fa-file-signature nav-icon"></i>
                                <p>Generar orden</p>
                              </a>
                            </li>
                            <li class="nav-item">
                              <a href="/results/view" class="nav-link">
                                <i class="fa-solid fa-file-pdf nav-icon"></i>
                                <p>Resultados</p>
                              </a>
                            </li>
                            <% } else if(locals.userLogged.id_rol == 2) { %>
                                <li class="nav-item">
                                  <a href="/results/view" class="nav-link">
                                    <i class="fa-solid fa-file-pdf nav-icon"></i>
                                    <p>Resultados</p>
                                  </a>
                                </li>
                            <% } %>
                          <% } %>
                      </ul>
                    </li>
          </ul>
        </nav>
        <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
</aside>